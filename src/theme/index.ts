import { createStitches } from '@stitches/react'

export const stitches = createStitches({})

export const applyGlobalStyles = stitches.globalCss({
  body: {
    // background: 'rgba(0,0,0,0.5)'
    // backdropFilter: 'blur(20px)'
  }
})
